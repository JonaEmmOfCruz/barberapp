{
  "name": "barberapp-backend",
  "version": "1.0.0",
  "description": "Backend para BarberApp",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "switch:user1": "node switch-user.js",
    "status": "node -e \"const fs = require('fs'); const env = fs.readFileSync('.env', 'utf8'); const match = env.match(/ACTIVE_USER=(.*)/); console.log('Usuario activo:', match ? match[1] : '1');\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}